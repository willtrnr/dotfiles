set -g status-bg blue
set -g status-fg white

set -gs escape-time 10

set -g focus-events on

set -g default-terminal 'screen-256color'
set -ga terminal-overrides ',kitty:Tc,alacritty:Tc,putty-256color:Tc'
